// Removing main div element
const element = document.getElementById('main');
element.remove(element);

// Adding h1 as newHeader variable to the body
const newHeader = document.createElement('h1');

// Setting id attribute to h1
newHeader.id = 'victory';

// Setting newHeader textContent to 'I am the champion'
newHeader.textContent = 'Subira Wafubwa is the champion';
document.append.newHeader;